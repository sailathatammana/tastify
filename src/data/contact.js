export const inputName = {
  type: "text",
  placeholder: "John Doe",
  required: true,
};

export const inputEmail = {
  type: "email",
  placeholder: "john@email.com",
  required: true,
};

export const inputDate = {
  type: "date",
  plceholder: "DD.MM.YYYY",
  required: true,
};

export const inputTime = {
  type: "time",
  plceholder: "HH.MM",
  required: true,
};
